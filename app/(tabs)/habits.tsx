import { Text, View } from "react-native";

export default function Habits() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Habits.</Text>
    </View>
  );
}
