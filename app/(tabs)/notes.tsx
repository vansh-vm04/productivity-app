import { Text, View } from "react-native";

export default function Notes() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Notes.</Text>
    </View>
  );
}
