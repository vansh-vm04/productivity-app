import { Text, View } from "react-native";

export default function Streaks() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Streaks.</Text>
    </View>
  );
}
